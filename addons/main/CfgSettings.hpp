
class CfgSettings {
    class CBA {
        class Versioning {
            class CFPU {
                class dependencies {
                    //CFP will hard exit if this is missing
                    CBA[] = {"cba_main", REQUIRED_CBA_VERSION, "(true)"};
                };
            };
        };
    };
};
